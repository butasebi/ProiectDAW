import { LineHoverDirective } from './line-hover.directive';

describe('LineHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LineHoverDirective();
    expect(directive).toBeTruthy();
  });
});
